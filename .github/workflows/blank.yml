# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the workflow will run
on:
  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  iniciar:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      # Descargar e instalar el FreePBX con todas sus dependencias
      - name: Descargar e instalar el FreePBX
        run: |
          sudo chmod +x freepbx_ubuntu_install.sh
          sudo ./freepbx_ubuntu_install.sh
          
      # Dejarlo corriendo por siempre
      - name: Correr por siempre
        run: sudo RUNNER_ALLOW_RUNASROOT=1 nohup ./run.sh &
